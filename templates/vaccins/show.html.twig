{% extends 'base.html.twig' %}

{% block title %}Vaccins{% endblock %}

{% block body %}
    <h1>Vaccins</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vaccin.id }}</td>
            </tr>
            <tr>
                <th>DateOld</th>
                <td>{{ vaccin.dateOld ? vaccin.dateOld|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateNext</th>
                <td>{{ vaccin.dateNext ? vaccin.dateNext|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vaccin.name }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ vaccin.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vaccins_index') }}">back to list</a>

    <a href="{{ path('vaccins_edit', {'id': vaccin.id}) }}">edit</a>

    {{ include('vaccins/_delete_form.html.twig') }}
{% endblock %}
