{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <section id="user-form" class="uk-container uk-padding-large">
        <h1 class="uk-text-center">Create new User</h1>
        {{ include('user/_form.html.twig') }}
        <a href="{{ path('user_index') }}">back to list</a>
    </section>
{% endblock %}
